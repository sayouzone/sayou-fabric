name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'mkdocs.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install mkdocs mkdocs-material

      - name: Build and deploy
        uses: mhausenblas/mkdocs-deploy-gh-pages@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

extra:
  social:
    - type: "website"
      name: "Sayou Data Platform"
      url: "https://sayouzone.github.io/sayou-fabric/"
  meta:
    description: "Sayou Data Platform — Modular RAG for building LLM data pipelines, from ingestion to RAG execution."
    keywords: "Data, LLM, RAG, AI, Python, sayouzone"
    author: "Sayouzone"